import Foundation

// I'm using a simple struct to store strings for home scene. For real world scenario we can use a L10n file.

struct TodoHomeStrings {
    private init() {}

    static let sceneTitle = "Todos"
    static let emptyTodos = "You don't have todos"
}
